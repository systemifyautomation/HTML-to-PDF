# HTML-to-PDF
In this project, we will setup an automation that will enable you to generate PDFs from HTML codes. This is very useful if you want to transform emails to PDFs (for invoices and whatnot). We'll be using Python to build the code and then we'll run the code on demand using HTTP requests
